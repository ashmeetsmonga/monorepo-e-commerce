const colors = require("tailwindcss/colors");

module.exports = {
  content: [
    // app content
    `components/**/*.{js,ts,jsx,tsx}`,
    // `/**/*.{js,ts,jsx,tsx}`,
    // include packages if not transpiling
    // "../../ui/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {},
  plugins: [],
};
